# =========================
# Tomcat
# =========================
server.tomcat.threads.max=50
server.tomcat.accept-count=50

# =========================
# HikariCP
# =========================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# =========================
# Database (local)
# =========================
spring.datasource.url=jdbc:mariadb://localhost:3308/busroutedb?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=smartbus
# spring.datasource.username=root
spring.datasource.password=plRovidb25!
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# =========================
# dispatch
# =========================
dispatch.fixed-delay-ms=1000
dispatch.batch-size=20
dispatch.stuck-sec=30
dispatch.retry-delay-sec=10
dispatch.max-retry=1

# =========================
# Mqtt
# =========================
mqtt.broker=tcp://10.10.10.77:1883
mqtt.username=smartbusmng-dispatch
mqtt.password=plDispatch25!
mqtt.clientIdPrefix=smartbusmng-dispatch-01
mqtt.qos=1
mqtt.connectTimeoutSec=10
mqtt.keepAliveSec=30

# =========================
# File Service
# =========================
file.service.base.url=http://localhost:8090/api/v2/attachments
